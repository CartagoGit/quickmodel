# Model Context Protocol (MCP) Server

QuickModel includes a fully standard-compliant **MCP Server** that enables AI assistants (like Claude, Cursor, or Antigravity) to deeply understand and interact with the library.

By connecting an AI agent to this server, you enable it to:

- **Generate valid QModel classes** automatically.
- **Validate your code** for common errors.
- **Simulate data transformations** ensuring correctness.
- **Inspect model structures** and explain behaviors.

## Installation & Setup

You can run the MCP server directly via `npx` (for users) or from the source (for contributors).

### For Users (Public)

To use QuickModel tools in your AI editor:

```bash
npx -y @cartago-git/quickmodel mcp
```

### For Contributors (Internal)

If you are developing QuickModel itself:

```bash
bun run mcp
```

## Editor Integration

We provide a helper script to generate configuration for common editors:

```bash
# In the project root
bun run scripts/generate-mcp-config.ts
```

### Antigravity IDE

1. Open the **Agent** panel.
2. Click the `...` menu > **Manage MCP Servers**.
3. Select **View raw config** (`mcp_config.json`).
4. Add the configuration generated by the script above.

### Cursor / VSCode / Claude Desktop

Follow the instructions provided by the `generate-mcp-config.ts` script to update your respective configuration files.

## Available Tools

The server exposes tools divided into two categories:

- **[Public Tools](./public/index.md)**: For users building applications with QuickModel.
- **[Internal Tools](./internal/index.md)**: For maintainers developing QuickModel itself.
